import skillsReducer from './reducer';

describe('skillsReducer', () => {
  xit('update skills with value', () => {

  });

  xit('should not allow to update when there is no more available points', () => {

  });
});
