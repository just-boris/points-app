import React from 'react';
import {shallow} from 'enzyme';
import Slider from './';

describe('<Slider />', () => {
  function Page(component) {
  }

  function createComponent() {
  }

  xit('should render slider', () => {

  });

  xit('should call handler when value was changed', () => {

  });
});
